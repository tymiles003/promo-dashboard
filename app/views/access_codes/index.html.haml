%p#notice= notice
%h1 Listing Access Codes
%table
  %thead
    %tr
      %th User
      %th Code
      %th Invitee name
      %th Invitee url
      %th Invitee info
      %th{:colspan => "3"}
  %tbody
    - @access_codes.each do |access_code|
      %tr
        %td= access_code.user
        %td= access_code.code
        %td= access_code.invitee_name
        %td= access_code.invitee_url
        %td= access_code.invitee_info
        %td= link_to 'Show', access_code
        %td= link_to 'Edit', edit_access_code_path(access_code)
        %td= link_to 'Destroy', access_code, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Access code', new_access_code_path
